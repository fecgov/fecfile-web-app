import { TransactionNavigationControls } from './transaction-navigation-controls.model';

describe('TransactionNavigationControls', () => {
  it('should create an instance', () => {
    expect(new TransactionNavigationControls()).toBeTruthy();
  });
});
