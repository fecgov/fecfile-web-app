applications:
  - name: fecfile-web-app
    instances: 1
    memory: 128M
    buildpack: nginx_buildpack

    routes:
      - route:  fecfile-web-app-stage.app.cloud.gov

    env:
      EXAMPLE: example
